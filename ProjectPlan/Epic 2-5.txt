```json
{
  "id": 5,
  "description": "IPlayerActionProvider를 구현하는 로컬 유저 입력 Gateway와, 이를 위한 액션 버튼 UI(View)를 함께 구축한다. 액션 UI는 Fold·Check·Call·Raise(Bet) 버튼과 레이즈 금액 슬라이더(최소 레이즈 ~ 올인)로 구성되며, Director가 해당 플레이어의 턴을 요청할 때만 활성화된다. 현재 콜 금액, 최소 레이즈 금액, 남은 칩 등 맥락 정보를 버튼 레이블에 반영한다(예: 'Call 200', 'Raise to 600'). 유저가 버튼을 클릭하면 Gateway가 async 대기를 해제하며 선택된 액션을 Director에 반환한다. 유효하지 않은 액션 조합은 버튼 비활성화로 사전 차단한다. 에픽 완료 시 AI 1명과 로컬 플레이어 1명이 참여하는 최소 게임에서 버튼 조작으로 한 핸드를 완료할 수 있음을 검증한다.",
  "epic": [
    {
      "id": 1,
      "task": "PlayerActionContext 정의",
      "layer": "Entity",
      "description": "로컬 플레이어에게 턴 결정을 요청할 때 전달하는 맥락 데이터 클래스. 필드: int CurrentBetToCall(콜에 필요한 추가 금액), int MinRaiseAmount(최소 레이즈 총액), int MaxRaiseAmount(올인 시 총액), int PlayerChips(플레이어 잔여 칩), int PotTotal(현재 팟 합계), HashSet<PlayerActionType> ValidActions(현 상황에서 허용되는 액션 타입 집합). 이 클래스는 순수 데이터 컨테이너이며 불변(immutable)으로 설계한다."
    },
    {
      "id": 2,
      "task": "ActionValidationUsecase 작성",
      "layer": "Usecase",
      "description": "현재 라운드 상태(Entity)를 입력받아 PlayerActionContext를 생성·반환하는 유스케이스 클래스. 로직 규칙: (1) 현재 베팅이 플레이어 기존 베팅과 같으면 Check 허용, 아니면 Call 허용. (2) Call 금액이 플레이어 잔여 칩 이상이면 Call 대신 All-In만 허용하고 Raise 비활성. (3) Raise 가능 시 MinRaise = 직전 레이즈 크기(또는 빅블라인드) 이상, MaxRaise = 플레이어 칩 전량. (4) Fold는 항상 허용. 결과로 ValidActions 집합과 금액 파라미터를 채운 PlayerActionContext를 반환한다."
    },
    {
      "id": 3,
      "task": "LocalPlayerInputGateway 작성",
      "layer": "Gateway",
      "description": "IPlayerActionProvider 인터페이스를 구현하는 Gateway 클래스. 내부에 TaskCompletionSource<PlayerAction>을 보유한다. RequestActionAsync(PlayerActionContext) 호출 시 새 TaskCompletionSource를 생성하고 await로 대기한다. 외부(Director)에서 SubmitAction(PlayerAction)을 호출하면 TaskCompletionSource.SetResult로 대기를 해제하여 선택된 액션을 반환한다. CancellationToken을 지원하여 타임아웃·게임 중단 시 취소 가능하게 한다. MonoBehaviour를 상속하지 않는 순수 C# 클래스로 구현한다."
    },
    {
      "id": 4,
      "task": "ActionPanelView 작성",
      "layer": "View",
      "description": "MonoBehaviour 기반 UI View 클래스. 씬의 Canvas 하위에 배치된다. 참조 필드: Button foldButton, Button checkButton, Button callButton, Button raiseButton, Slider raiseSlider, TMP_Text callLabel, TMP_Text raiseLabel, TMP_Text sliderValueLabel, GameObject panelRoot. 공개 메서드: (1) Show(PlayerActionContext ctx) — panelRoot 활성화, ctx의 ValidActions에 따라 각 버튼 interactable 설정, callLabel에 'Call {amount}' 표시, raiseSlider의 minValue/maxValue를 ctx.MinRaiseAmount/MaxRaiseAmount로 설정, raiseLabel 갱신. (2) Hide() — panelRoot 비활성화. 슬라이더 값 변경 시 raiseLabel과 sliderValueLabel을 실시간 갱신('Raise to {value}'). 각 버튼 클릭과 최종 레이즈 금액을 외부에 전달하기 위해 System.Action<PlayerActionType, int> OnActionSelected 콜백을 노출한다."
    },
    {
      "id": 5,
      "task": "Action Panel UI 레이아웃 구성",
      "layer": "None",
      "description": "Unity 에디터에서 ActionPanelView가 사용할 UI 프리팹(또는 씬 내 오브젝트)을 구성한다. Canvas 하위에 Panel(panelRoot)을 만들고, 하단 고정 레이아웃으로 Fold·Check·Call·Raise 네 개의 Button(TextMeshPro 레이블 포함)을 가로 배치한다. Raise 버튼 상단에 Slider(최소~올인 범위)와 현재 금액 텍스트를 배치한다. 각 UI 요소를 ActionPanelView 컴포넌트의 직렬화 필드에 드래그-연결한다. 해상도 1920×1080 기준 앵커·피봇을 설정하여 하단 중앙 정렬한다."
    },
    {
      "id": 6,
      "task": "LocalPlayerActionDirector 작성",
      "layer": "Director",
      "description": "로컬 플레이어의 턴 흐름을 중재하는 Director 클래스. 의존성: ActionValidationUsecase, LocalPlayerInputGateway, ActionPanelView(인터페이스 또는 콜백으로 주입). 핵심 메서드 HandleTurnAsync(RoundState, Player): (1) ActionValidationUsecase로 PlayerActionContext 생성. (2) ActionPanelView.Show(ctx) 호출하여 UI 표시. (3) View의 OnActionSelected 콜백을 구독하여, 유저 클릭 시 PlayerAction 객체를 구성하고 LocalPlayerInputGateway.SubmitAction() 호출. (4) Gateway.RequestActionAsync()를 await하여 결과 PlayerAction 수신. (5) ActionPanelView.Hide() 호출. (6) 결과를 게임 라운드 Director에 반환. View 콜백 구독/해제를 HandleTurnAsync 진입/종료 시 정확히 관리하여 중복 호출을 방지한다."
    },
    {
      "id": 7,
      "task": "기존 게임 라운드 Director에 로컬 플레이어 경로 통합",
      "layer": "Director",
      "description": "기존 라운드 진행 Director(에픽 4에서 구현)의 플레이어 액션 요청 분기에 로컬 플레이어 분기를 추가한다. 플레이어의 IPlayerActionProvider가 LocalPlayerInputGateway 타입이면 LocalPlayerActionDirector.HandleTurnAsync()를 호출하도록 분기하거나, 기존 다형성 호출이 이미 Gateway 인터페이스를 통해 동작하는 경우 LocalPlayerInputGateway를 등록만 하면 된다. AI와 로컬 플레이어가 혼재하는 플레이어 리스트를 올바르게 순회하는지 확인한다."
    },
    {
      "id": 8,
      "task": "통합 검증: AI 1명 + 로컬 플레이어 1명 최소 핸드 플레이",
      "layer": "None",
      "description": "테스트 씬을 구성하여 AI 플레이어 1명과 로컬 플레이어 1명이 참여하는 2인 헤즈업 게임을 실행한다. 검증 항목: (1) 로컬 플레이어 턴에만 액션 패널이 활성화되고 AI 턴에는 비활성 상태 유지. (2) 현재 상황에 맞는 버튼만 interactable(예: 첫 액션 시 Check 불가하면 비활성). (3) Call/Raise 레이블에 올바른 금액 표시. (4) 슬라이더로 레이즈 금액 조정 후 Raise 클릭 시 해당 금액 반영. (5) Fold 선택 시 즉시 핸드 종료되고 AI 승리. (6) 쇼다운까지 진행하여 승자 판정 및 팟 지급 정상 동작. (7) 콘솔 에러·예외 없이 한 핸드 전체가 완료됨."
    }
  ]
}
```
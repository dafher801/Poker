```json
{
  "id": 1,
  "description": "Director → View 간 통신을 위한 출력 추상화 계층을 구축한다. IGameEventBroadcaster 인터페이스를 정의하고, 로컬(인프로세스) 구현체인 LocalGameEventBroadcaster를 작성한다. 게임 진행 중 발생하는 모든 이벤트 타입을 Entity로 정의한다: 페이즈 변경(PhaseChanged), 카드 딜(CardsDealt — 홀카드·커뮤니티 구분), 플레이어 액션 수행(PlayerActed), 팟 갱신(PotUpdated), 턴 시작(TurnStarted), 쇼다운 결과(ShowdownResult), 핸드 종료(HandEnded). View는 이 인터페이스만 구독하므로, 이후 네트워크 구현체로 교체해도 View 코드를 수정할 필요가 없다. 에픽 완료 시 단위 테스트로 이벤트 발행·구독·수신이 정상 동작함을 확인한다.",
  "epic": [
    {
      "id": 1,
      "task": "게임 이벤트 기본 타입 정의",
      "layer": "Entity",
      "description": "GameEventBase.cs — 모든 게임 이벤트의 공통 기반이 되는 추상 클래스 GameEventBase를 정의한다. 필드: long Timestamp(이벤트 발생 시각, UTC Ticks), string HandId(현재 핸드 식별자). 이 클래스는 sealed가 아니며 후속 태스크에서 정의하는 구체 이벤트들이 상속한다."
    },
    {
      "id": 2,
      "task": "PhaseChangedEvent 정의",
      "layer": "Entity",
      "description": "PhaseChangedEvent.cs — GameEventBase를 상속한다. 필드: BettingPhase PreviousPhase, BettingPhase CurrentPhase. BettingPhase는 같은 파일 내에 enum으로 정의하며, 값은 PreFlop, Flop, Turn, River, Showdown 5개이다(마일스톤 1에서 이미 정의되어 있다면 해당 Entity를 재사용하고, 이 파일에서는 enum 정의를 생략한다)."
    },
    {
      "id": 3,
      "task": "CardsDealtEvent 정의",
      "layer": "Entity",
      "description": "CardsDealtEvent.cs — GameEventBase를 상속한다. 같은 파일에 enum CardDealType { HoleCard, CommunityFlop, CommunityTurn, CommunityRiver }을 정의한다. 필드: CardDealType DealType, IReadOnlyList<Card> Cards(딜된 카드 목록, M1의 Card 엔티티 사용), int TargetPlayerSeatIndex(홀카드일 때 대상 플레이어 좌석 인덱스, 커뮤니티 카드일 때 -1)."
    },
    {
      "id": 4,
      "task": "PlayerActedEvent 정의",
      "layer": "Entity",
      "description": "PlayerActedEvent.cs — GameEventBase를 상속한다. 필드: int SeatIndex(행동한 플레이어 좌석 인덱스), PlayerActionType ActionType(M1의 PlayerAction enum — Fold, Check, Call, Raise, AllIn 재사용), int Amount(베팅·레이즈·콜 금액, Fold·Check 시 0)."
    },
    {
      "id": 5,
      "task": "PotUpdatedEvent 정의",
      "layer": "Entity",
      "description": "PotUpdatedEvent.cs — GameEventBase를 상속한다. 필드: int MainPot(메인 팟 총액), IReadOnlyList<int> SidePots(사이드 팟 목록, 없으면 빈 리스트)."
    },
    {
      "id": 6,
      "task": "TurnStartedEvent 정의",
      "layer": "Entity",
      "description": "TurnStartedEvent.cs — GameEventBase를 상속한다. 필드: int SeatIndex(행동 차례가 된 플레이어 좌석 인덱스), IReadOnlyList<PlayerActionType> AvailableActions(해당 플레이어가 선택 가능한 액션 목록), int MinRaiseAmount, int MaxRaiseAmount(레이즈 허용 범위, 레이즈 불가 시 둘 다 0), float TimeLimit(행동 제한 시간(초))."
    },
    {
      "id": 7,
      "task": "ShowdownResultEvent 정의",
      "layer": "Entity",
      "description": "ShowdownResultEvent.cs — GameEventBase를 상속한다. 같은 파일에 내부 클래스 ShowdownEntry를 정의한다. ShowdownEntry 필드: int SeatIndex, IReadOnlyList<Card> HoleCards, HandRank Rank(M1의 핸드 랭크 엔티티 재사용), bool IsWinner. ShowdownResultEvent 필드: IReadOnlyList<ShowdownEntry> Entries."
    },
    {
      "id": 8,
      "task": "HandEndedEvent 정의",
      "layer": "Entity",
      "description": "HandEndedEvent.cs — GameEventBase를 상속한다. 같은 파일에 내부 클래스 PotAward를 정의한다. PotAward 필드: int SeatIndex, int Amount, string PotLabel(\"Main\"/\"Side1\" 등). HandEndedEvent 필드: IReadOnlyList<PotAward> Awards, HandEndReason Reason. 같은 파일에 enum HandEndReason { Showdown, LastManStanding }을 정의한다."
    },
    {
      "id": 9,
      "task": "IGameEventBroadcaster 인터페이스 정의",
      "layer": "Director",
      "description": "IGameEventBroadcaster.cs — 제네릭 이벤트 발행·구독 인터페이스를 정의한다. 메서드: void Subscribe<T>(Action<T> handler) where T : GameEventBase, void Unsubscribe<T>(Action<T> handler) where T : GameEventBase, void Publish<T>(T gameEvent) where T : GameEventBase. Director는 Publish만 호출하고 View는 Subscribe/Unsubscribe만 호출하는 것이 의도된 사용 패턴이지만, 인터페이스 자체는 분리하지 않고 단일로 유지한다."
    },
    {
      "id": 10,
      "task": "LocalGameEventBroadcaster 구현",
      "layer": "Director",
      "description": "LocalGameEventBroadcaster.cs — IGameEventBroadcaster를 구현하는 인프로세스(로컬) 클래스. 내부에 Dictionary<Type, Delegate>를 사용하여 이벤트 타입별 구독자 목록을 관리한다. Subscribe<T>: 딕셔너리에 Action<T>를 추가(멀티캐스트 델리게이트 체인). Unsubscribe<T>: 델리게이트 체인에서 해당 핸들러 제거. Publish<T>: 해당 타입 키가 존재하면 등록된 모든 핸들러를 동기적으로 호출. 핸들러 실행 중 예외 발생 시 try-catch로 감싸 다른 핸들러 실행에 영향을 주지 않도록 하고, Debug.LogException으로 기록한다. 스레드 안전성은 요구하지 않는다(메인 스레드 전용)."
    },
    {
      "id": 11,
      "task": "이벤트 발행·구독·수신 단위 테스트 작성",
      "layer": "None",
      "description": "Tests/EditMode/GameEventBroadcasterTests.cs — Unity Test Framework(Edit Mode)를 사용한다. 최소 테스트 케이스: (1) Subscribe 후 Publish하면 핸들러가 호출되는지 확인, (2) Unsubscribe 후 Publish하면 핸들러가 호출되지 않는지 확인, (3) 서로 다른 이벤트 타입 Subscribe 시 해당 타입만 수신하는지 확인, (4) 동일 이벤트 타입에 복수 핸들러 등록 시 모두 호출되는지 확인, (5) 핸들러 내부에서 예외 발생 시 다른 핸들러가 정상 호출되는지 확인. 모든 7개 이벤트 타입(PhaseChanged ~ HandEnded)에 대해 Publish-Subscribe 라운드트립 테스트를 포함한다."
    }
  ]
}
```
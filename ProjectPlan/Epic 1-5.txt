```json
{
  "id": 5,
  "description": "Usecase 계층. 단일 베팅 라운드(프리플롭·플롭·턴·리버 중 하나)의 전체 흐름을 처리하는 BettingRoundUsecase를 구현한다. (1) 현재 상태에서 특정 플레이어가 수행 가능한 합법 액션 목록과 최소/최대 레이즈 금액을 산출하는 ActionValidator. (2) 플레이어 액션을 적용하여 해당 플레이어의 칩·베팅액·상태를 갱신하는 ActionExecutor. (3) 액션 순서 결정(프리플롭은 BB 다음, 이후 라운드는 SB/딜러 다음부터), 라운드 종료 조건 판정(모든 액티브 플레이어가 동일 금액을 베팅했거나 올인·폴드), 1명만 남았을 때 즉시 핸드 종료 판정을 포함하는 라운드 루프. IPlayerActionProvider를 통해 각 플레이어의 액션을 비동기로 수신하고, IGameEventPublisher로 각 액션을 발행한다.",
  "epic": [
    {
      "id": 1,
      "task": "IPlayerActionProvider 인터페이스 정의",
      "layer": "Usecase",
      "description": "IPlayerActionProvider.cs — Usecase 계층이 플레이어의 액션을 비동기로 요청·수신하기 위해 사용하는 인터페이스. 메서드 시그니처: UniTask<PlayerAction> RequestActionAsync(int seatIndex, IReadOnlyList<ActionType> legalActions, int minRaise, int maxRaise, int callAmount, CancellationToken ct). PlayerAction은 (ActionType type, int raiseAmount) 형태의 구조체로 같은 파일에 정의한다. ActionType은 Fold, Check, Call, Raise, AllIn을 포함하는 enum이며, 이미 Entity 계층에 정의되어 있다면 참조만 한다. 이 인터페이스의 실제 구현은 Gateway 계층(AI 입력, 로컬 UI 입력, 네트워크 입력)에서 이루어지므로 여기서는 계약만 선언한다."
    },
    {
      "id": 2,
      "task": "IGameEventPublisher 인터페이스 정의",
      "layer": "Usecase",
      "description": "IGameEventPublisher.cs — Usecase가 베팅 관련 이벤트를 외부(Director/View)에 알리기 위해 사용하는 인터페이스. 최소 다음 메서드를 포함한다: void PublishPlayerActed(int seatIndex, PlayerAction action, int chipAfter, int potTotal) — 플레이어 액션 발생 시 호출. void PublishBettingRoundStarted(BettingRoundType roundType) — 베팅 라운드(프리플롭/플롭/턴/리버) 시작 시 호출. void PublishBettingRoundEnded(BettingRoundType roundType) — 라운드 종료 시 호출. void PublishHandEndedByFold(int winningSeatIndex, int potAmount) — 1명만 남아 즉시 핸드 종료 시 호출. BettingRoundType은 Preflop, Flop, Turn, River를 포함하는 enum이며 같은 파일 또는 Entity에 정의한다. 실제 구현은 Gateway/Director 계층에서 한다."
    },
    {
      "id": 3,
      "task": "ActionValidator 구현",
      "layer": "Usecase",
      "description": "ActionValidator.cs — 순수 C# 클래스. 정적 혹은 인스턴스 메서드 GetLegalActions를 제공한다. 입력: 해당 플레이어의 현재 칩(stack), 해당 플레이어가 이번 라운드에 이미 베팅한 금액(currentBet), 라운드 내 현재 최고 베팅액(highestBet), 이전 레이즈 크기(lastRaiseSize), 빅블라인드 크기(bigBlind). 출력: (IReadOnlyList<ActionType> legalActions, int callAmount, int minRaise, int maxRaise). 로직 상세: (a) Fold는 항상 허용. (b) highestBet == currentBet이면 Check 허용, 아니면 불허. (c) Call: 필요 콜 금액 = highestBet − currentBet; 스택이 이 금액 이상이면 Call 허용, 미만이면 AllIn만 허용. (d) Raise: minRaise = highestBet + max(lastRaiseSize, bigBlind); maxRaise = currentBet + stack(올인 레이즈); 스택이 minRaise − currentBet 이상이면 Raise 허용. (e) AllIn: 스택 > 0이면 항상 허용. 단위 테스트 작성이 용이하도록 외부 의존성 없이 순수 함수로 구현한다."
    },
    {
      "id": 4,
      "task": "ActionExecutor 구현",
      "layer": "Usecase",
      "description": "ActionExecutor.cs — 순수 C# 클래스. 메서드 Execute(PlayerSeatState player, PlayerAction action, RoundBettingState roundState)를 제공한다. PlayerSeatState는 (stack, currentBet, status) 등 개별 시트의 가변 상태를 담은 Entity 혹은 DTO이다. RoundBettingState는 (highestBet, lastRaiseSize, potTotal) 등 라운드 전체 베팅 상태를 담은 DTO이다. 각 액션별 처리: (a) Fold → player.status = Folded. (b) Check → 상태 변경 없음(검증은 ActionValidator 책임). (c) Call → 콜 금액만큼 player.stack 차감, player.currentBet += 콜 금액, roundState.potTotal += 콜 금액. (d) Raise → 레이즈 총액(action.raiseAmount − player.currentBet)만큼 stack 차감, roundState.highestBet = action.raiseAmount, roundState.lastRaiseSize = action.raiseAmount − 이전 highestBet, potTotal 갱신. (e) AllIn → 남은 stack 전부 투입, 금액에 따라 highestBet 갱신 여부 결정, player.status = AllIn. 모든 연산 후 stack이 음수가 되지 않도록 Entity 레벨 검증을 전제로 한다."
    },
    {
      "id": 5,
      "task": "TurnOrderResolver 구현",
      "layer": "Usecase",
      "description": "TurnOrderResolver.cs — 순수 C# 클래스. 메서드 ResolveOrder(IReadOnlyList<PlayerSeatState> seats, int dealerIndex, BettingRoundType roundType)를 제공하여, 해당 라운드에서 액션해야 할 시트 인덱스의 순서 리스트를 반환한다. 로직: (a) 프리플롭(Preflop): 빅블라인드(BB) 다음 좌석(UTG)부터 시계 방향으로 순회하여 status가 Active 또는 아직 행동하지 않은 플레이어를 포함한다. BB는 마지막에 배치. (b) 플롭/턴/리버: 딜러(SB 위치 포함) 다음 좌석부터 시계 방향으로 Active 플레이어만 포함. (c) Folded 또는 AllIn 상태인 플레이어는 제외. 좌석 수가 2명(헤즈업)일 때의 특수 규칙도 처리한다: 프리플롭에서는 딜러(=SB)가 먼저, 포스트플롭에서는 딜러가 아닌 쪽이 먼저."
    },
    {
      "id": 6,
      "task": "RoundEndEvaluator 구현",
      "layer": "Usecase",
      "description": "RoundEndEvaluator.cs — 순수 C# 클래스. 두 가지 판정 메서드를 제공한다. (1) bool IsBettingRoundComplete(IReadOnlyList<PlayerSeatState> seats, int highestBet) — 모든 Active 상태 플레이어가 이번 라운드에서 한 번 이상 행동했고(hasActed 플래그), 각 플레이어의 currentBet이 highestBet과 같거나 AllIn/Folded 상태인 경우 true 반환. 프리플롭 BB의 옵션(아무도 레이즈하지 않았을 때 BB가 체크/레이즈 선택)도 고려한다. (2) bool IsOnlyOnePlayerRemaining(IReadOnlyList<PlayerSeatState> seats, out int winningSeatIndex) — Folded가 아니고 AllIn도 아닌 Active 플레이어가 1명 이하일 때(AllIn 플레이어만 여럿 남은 경우 제외) 핸드 즉시 종료를 판정한다. 정확히는 Folded가 아닌 플레이어가 1명만 남으면 true."
    },
    {
      "id": 7,
      "task": "BettingRoundUsecase 구현",
      "layer": "Usecase",
      "description": "BettingRoundUsecase.cs — 단일 베팅 라운드의 전체 루프를 오케스트레이션하는 클래스. 생성자에서 IPlayerActionProvider와 IGameEventPublisher를 주입받는다. 비동기 메서드 UniTask<BettingRoundResult> ExecuteRoundAsync(IReadOnlyList<PlayerSeatState> seats, int dealerIndex, BettingRoundType roundType, RoundBettingState roundState, CancellationToken ct)를 제공한다. 내부 흐름: (1) IGameEventPublisher.PublishBettingRoundStarted 호출. (2) TurnOrderResolver로 초기 액션 순서 생성. (3) 루프 시작 — 현재 순서의 플레이어에 대해 ActionValidator.GetLegalActions로 합법 액션 산출 → IPlayerActionProvider.RequestActionAsync로 액션 수신 → ActionExecutor.Execute로 상태 반영 → IGameEventPublisher.PublishPlayerActed 호출. (4) 액션 적용 후 RoundEndEvaluator.IsOnlyOnePlayerRemaining 체크 → true면 PublishHandEndedByFold 호출 후 BettingRoundResult.HandEndedByFold 반환. (5) 레이즈 발생 시 해당 플레이어 이후 아직 행동하지 않은 플레이어를 다시 순서에 추가(리레이즈 대응). (6) RoundEndEvaluator.IsBettingRoundComplete가 true가 되면 루프 종료. (7) PublishBettingRoundEnded 호출 후 BettingRoundResult.RoundComplete 반환. BettingRoundResult는 enum(RoundComplete, HandEndedByFold)과 winningSeatIndex(폴드 승리 시)를 포함하는 구조체로 같은 파일에 정의한다."
    }
  ]
}
```


```json
{
  "id": 1,
  "description": "텍사스 홀덤에 필요한 모든 데이터 모델(Entity)과 외부 의존성 추상화(Gateway 인터페이스 + 인메모리/테스트용 구현체)를 정의한다.",
  "epic": [
    {
      "id": 1,
      "task": "Suit 열거형 정의",
      "layer": "Entity",
      "description": "Suit.cs — Spade, Heart, Diamond, Club 네 가지 값을 갖는 enum을 정의한다. 네임스페이스는 TexasHoldem.Entity로 한다."
    },
    {
      "id": 2,
      "task": "Rank 열거형 정의",
      "layer": "Entity",
      "description": "Rank.cs — Two(2)부터 Ace(14)까지 13개의 값을 갖는 enum을 정의한다. 정수 값을 명시적으로 할당하여 비교 연산에 활용할 수 있도록 한다."
    },
    {
      "id": 3,
      "task": "Card 값 객체 정의",
      "layer": "Entity",
      "description": "Card.cs — Suit와 Rank를 필드로 갖는 불변(immutable) 구조체 또는 클래스를 정의한다. Equals, GetHashCode를 오버라이드하여 동등성 비교를 지원하고, ToString()으로 'Ace of Spades' 등 가독성 있는 문자열을 반환한다. IEquatable<Card>를 구현한다."
    },
    {
      "id": 4,
      "task": "Deck 엔티티 정의",
      "layer": "Entity",
      "description": "Deck.cs — 생성 시 52장의 고유한 Card 리스트를 내부에 보유한다. Shuffle(IRandomSource) 메서드는 외부에서 주입받은 IRandomSource에 셔플을 위임한다. Draw() 메서드는 맨 위 카드를 제거하며 반환하되, 남은 카드가 0장이면 InvalidOperationException을 던진다. Remaining 프로퍼티로 잔여 카드 수를 반환한다. 중복 카드가 들어가지 않도록 생성 시 검증한다."
    },
    {
      "id": 5,
      "task": "PlayerStatus 열거형 정의",
      "layer": "Entity",
      "description": "PlayerStatus.cs — Waiting, Active, Folded, AllIn, Eliminated 다섯 가지 값을 갖는 enum을 정의한다."
    },
    {
      "id": 6,
      "task": "PlayerData 엔티티 정의",
      "layer": "Entity",
      "description": "PlayerData.cs — string Id, string Name, int Chips, List<Card> HoleCards, int CurrentBet, PlayerStatus Status, int SeatIndex 필드를 갖는 클래스를 정의한다. Chips 세터에서 음수 할당 시 ArgumentOutOfRangeException을 던진다. CurrentBet도 음수 불가. SeatIndex는 0~9 범위만 허용한다. HoleCards는 null이 아닌 빈 리스트로 초기화하며, 최대 2장까지만 추가 가능하도록 AddHoleCard 메서드에서 검증한다."
    },
    {
      "id": 7,
      "task": "ActionType 열거형 및 PlayerAction 값 객체 정의",
      "layer": "Entity",
      "description": "PlayerAction.cs — ActionType enum(Fold, Check, Call, Raise, AllIn)을 파일 상단 또는 별도 enum 파일에 정의한다. PlayerAction 클래스는 string PlayerId, ActionType Type, int Amount 필드를 갖는 불변 객체이다. 유효성 규칙: Fold·Check일 때 Amount는 반드시 0이어야 하고, Raise일 때 Amount는 1 이상이어야 하며, Call·AllIn일 때 Amount는 0 이상이어야 한다. 위반 시 ArgumentException을 던진다."
    },
    {
      "id": 8,
      "task": "BlindInfo 값 객체 정의",
      "layer": "Entity",
      "description": "BlindInfo.cs — int SmallBlind, int BigBlind 필드를 갖는 불변 객체이다. SmallBlind는 1 이상, BigBlind는 SmallBlind 이상이어야 한다. 위반 시 ArgumentException을 던진다."
    },
    {
      "id": 9,
      "task": "Pot 엔티티 정의",
      "layer": "Entity",
      "description": "Pot.cs — int Amount, List<string> EligiblePlayerIds 필드를 갖는 클래스이다. Amount는 0 이상이어야 하며, EligiblePlayerIds는 null 불가하고 빈 리스트 허용한다. AddAmount(int) 메서드에서 음수 추가를 방지한다."
    },
    {
      "id": 10,
      "task": "GamePhase 열거형 정의",
      "layer": "Entity",
      "description": "GamePhase.cs — PreFlop, Flop, Turn, River, Showdown 다섯 가지 값을 갖는 enum을 정의한다."
    },
    {
      "id": 11,
      "task": "GameState 엔티티 정의",
      "layer": "Entity",
      "description": "GameState.cs — List<PlayerData> Players, List<Card> CommunityCards, List<Pot> Pots, GamePhase Phase, int DealerIndex, BlindInfo Blinds, int CurrentPlayerIndex 필드를 갖는 클래스이다. Players는 최소 2명, 최대 10명이어야 한다. CommunityCards는 최대 5장까지 허용한다. DealerIndex와 CurrentPlayerIndex는 Players 범위 내여야 한다. 위반 시 적절한 예외를 던진다. 외부에서 스냅샷 용도로 DeepClone() 또는 복사 생성자를 제공한다."
    },
    {
      "id": 12,
      "task": "IRandomSource 게이트웨이 인터페이스 정의",
      "layer": "Gateway",
      "description": "IRandomSource.cs — void Shuffle<T>(IList<T> list) 메서드 하나를 갖는 인터페이스를 정의한다. 네임스페이스는 TexasHoldem.Gateway로 한다."
    },
    {
      "id": 13,
      "task": "IGameStateRepository 게이트웨이 인터페이스 정의",
      "layer": "Gateway",
      "description": "IGameStateRepository.cs — void Save(GameState state), GameState Load() 두 메서드를 갖는 인터페이스를 정의한다."
    },
    {
      "id": 14,
      "task": "IPlayerActionProvider 게이트웨이 인터페이스 정의",
      "layer": "Gateway",
      "description": "IPlayerActionProvider.cs — Task<PlayerAction> GetAction(string playerId, GameState snapshot, List<ActionType> legalActions) 메서드를 갖는 인터페이스를 정의한다. 비동기 반환으로 인간 입력 대기, AI 판단, 네트워크 수신 등 다양한 구현을 수용한다."
    },
    {
      "id": 15,
      "task": "IGameEventBroadcaster 게이트웨이 인터페이스 정의",
      "layer": "Gateway",
      "description": "IGameEventBroadcaster.cs — 다음 메서드를 갖는 인터페이스를 정의한다: void OnPhaseChanged(GamePhase phase), void OnPlayerActed(PlayerAction action), void OnCardsDealt(string playerId, List<Card> cards), void OnCommunityCardsRevealed(List<Card> cards), void OnPotUpdated(List<Pot> pots), void OnShowdown(List<PlayerData> players), void OnHandResult(List<string> winnerIds, List<Pot> pots)."
    },
    {
      "id": 16,
      "task": "SystemRandomSource 구현체 작성",
      "layer": "Gateway",
      "description": "SystemRandomSource.cs — IRandomSource를 구현한다. 내부에 System.Random 인스턴스를 보유하고, Shuffle<T>(IList<T>) 메서드에서 Fisher-Yates 알고리즘으로 리스트를 섞는다. 선택적으로 생성자에서 seed를 받을 수 있도록 한다."
    },
    {
      "id": 17,
      "task": "FixedRandomSource 테스트용 구현체 작성",
      "layer": "Gateway",
      "description": "FixedRandomSource.cs — IRandomSource를 구현한다. Shuffle 호출 시 리스트를 변경하지 않아 원래 순서를 유지한다. 테스트에서 카드 순서를 예측 가능하게 만드는 데 사용한다."
    },
    {
      "id": 18,
      "task": "InMemoryGameStateRepository 구현체 작성",
      "layer": "Gateway",
      "description": "InMemoryGameStateRepository.cs — IGameStateRepository를 구현한다. 내부 Dictionary<string, GameState> 또는 단일 GameState 필드에 Save로 저장하고 Load로 반환한다. Load 시 저장된 상태가 없으면 null을 반환하거나 InvalidOperationException을 던진다."
    },
    {
      "id": 19,
      "task": "StubPlayerActionProvider 테스트용 구현체 작성",
      "layer": "Gateway",
      "description": "StubPlayerActionProvider.cs — IPlayerActionProvider를 구현한다. 생성자 또는 EnqueueAction 메서드로 PlayerAction들을 Queue에 미리 적재하고, GetAction 호출 시 큐에서 순서대로 Dequeue하여 Task.FromResult로 반환한다. 큐가 비었을 때 호출하면 InvalidOperationException을 던진다."
    },
    {
      "id": 20,
      "task": "StubGameEventBroadcaster 테스트용 구현체 작성",
      "layer": "Gateway",
      "description": "StubGameEventBroadcaster.cs — IGameEventBroadcaster를 구현한다. 각 메서드 호출 시 이벤트 종류와 파라미터를 내부 List<(string EventName, object[] Args)> 형태의 로그 리스트에 기록만 한다. GetLog() 메서드로 기록된 이벤트를 조회할 수 있게 한다."
    },
    {
      "id": 21,
      "task": "Entity 계층 에디터 테스트 작성",
      "layer": "None",
      "description": "Tests/EditMode/EntityTests.cs — Unity Test Framework (EditMode)를 사용하여 다음을 검증하는 테스트를 작성한다: (1) Deck 생성 시 52장인지 확인, (2) Deck 생성 시 모든 카드가 고유한지 확인, (3) FixedRandomSource로 Shuffle 후 카드 무결성(52장, 중복 없음) 확인, (4) Draw 호출 시 Remaining 감소 확인, (5) 52회 Draw 후 추가 Draw 시 예외 발생 확인, (6) Card 동등성 비교(같은 Suit·Rank면 Equal, 다르면 Not Equal) 확인, (7) PlayerData Chips 음수 할당 시 예외, (8) PlayerAction 유효성 규칙 위반 시 예외, (9) BlindInfo 유효성 위반 시 예외, (10) GameState Players 수 범위 위반 시 예외."
    },
    {
      "id": 22,
      "task": "Gateway 스텁 에디터 테스트 작성",
      "layer": "None",
      "description": "Tests/EditMode/GatewayStubTests.cs — Unity Test Framework (EditMode)를 사용하여 다음을 검증하는 테스트를 작성한다: (1) SystemRandomSource.Shuffle이 리스트 원소를 유지하면서 순서를 변경하는지 확인, (2) FixedRandomSource.Shuffle이 순서를 변경하지 않는지 확인, (3) InMemoryGameStateRepository Save → Load 왕복 검증, (4) StubPlayerActionProvider 큐 동작 및 빈 큐 예외 검증, (5) StubGameEventBroadcaster 이벤트 로그 기록 검증."
    }
  ]
}
```